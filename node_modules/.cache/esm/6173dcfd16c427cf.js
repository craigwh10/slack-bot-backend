let express,db;_16c‍.w("express",[["default",["express"],function(v){express=v}]]);_16c‍.w("./db/db",[["default",["db"],function(v){db=v}]]);

// Set up the express app
const app = express();
// get all todos
app.get("/api/v1/responses", (req, res) => {
  res.status(200).send({
    success: "true",
    message: "todos retrieved successfully",
    responses: db
  });
});
const PORT = 5000;

app.listen(PORT, () => {
  console.log(`server running on port ${PORT}`);
});
